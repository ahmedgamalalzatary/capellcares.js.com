// Global brand defaults.

export const BRAND_NAME = "Đespacito Delight";

/** Latin-only form, for contexts that can't render the crossed D safely. */
export const BRAND_NAME_ASCII = "Despacito Delight";

// TODO(brand): replace with the live domain once confirmed.
export const FALLBACK_SITE_URL = "https://despacitodelight.com";
